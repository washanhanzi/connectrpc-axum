# Compare Repository

Compare an external GitHub repository against connectrpc-axum.

**Usage:** `/connectrpc-axum:compare-repo <github-url>`

Use the `compare-repo` skill to clone the repository and generate a comparison document.

The repository URL is: $ARGUMENTS
