// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: fortune.proto

package fortunev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/washanhanzi/connectrpc-axum/benchmarks/connect-go/gen/fortune/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// FortuneServiceName is the fully-qualified name of the FortuneService service.
	FortuneServiceName = "fortune.v1.FortuneService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// FortuneServiceGetFortunesProcedure is the fully-qualified name of the FortuneService's
	// GetFortunes RPC.
	FortuneServiceGetFortunesProcedure = "/fortune.v1.FortuneService/GetFortunes"
)

// FortuneServiceClient is a client for the fortune.v1.FortuneService service.
type FortuneServiceClient interface {
	GetFortunes(context.Context, *connect.Request[v1.GetFortunesRequest]) (*connect.Response[v1.GetFortunesResponse], error)
}

// NewFortuneServiceClient constructs a client for the fortune.v1.FortuneService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewFortuneServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) FortuneServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	fortuneServiceMethods := v1.File_fortune_proto.Services().ByName("FortuneService").Methods()
	return &fortuneServiceClient{
		getFortunes: connect.NewClient[v1.GetFortunesRequest, v1.GetFortunesResponse](
			httpClient,
			baseURL+FortuneServiceGetFortunesProcedure,
			connect.WithSchema(fortuneServiceMethods.ByName("GetFortunes")),
			connect.WithClientOptions(opts...),
		),
	}
}

// fortuneServiceClient implements FortuneServiceClient.
type fortuneServiceClient struct {
	getFortunes *connect.Client[v1.GetFortunesRequest, v1.GetFortunesResponse]
}

// GetFortunes calls fortune.v1.FortuneService.GetFortunes.
func (c *fortuneServiceClient) GetFortunes(ctx context.Context, req *connect.Request[v1.GetFortunesRequest]) (*connect.Response[v1.GetFortunesResponse], error) {
	return c.getFortunes.CallUnary(ctx, req)
}

// FortuneServiceHandler is an implementation of the fortune.v1.FortuneService service.
type FortuneServiceHandler interface {
	GetFortunes(context.Context, *connect.Request[v1.GetFortunesRequest]) (*connect.Response[v1.GetFortunesResponse], error)
}

// NewFortuneServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewFortuneServiceHandler(svc FortuneServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	fortuneServiceMethods := v1.File_fortune_proto.Services().ByName("FortuneService").Methods()
	fortuneServiceGetFortunesHandler := connect.NewUnaryHandler(
		FortuneServiceGetFortunesProcedure,
		svc.GetFortunes,
		connect.WithSchema(fortuneServiceMethods.ByName("GetFortunes")),
		connect.WithHandlerOptions(opts...),
	)
	return "/fortune.v1.FortuneService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case FortuneServiceGetFortunesProcedure:
			fortuneServiceGetFortunesHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedFortuneServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedFortuneServiceHandler struct{}

func (UnimplementedFortuneServiceHandler) GetFortunes(context.Context, *connect.Request[v1.GetFortunesRequest]) (*connect.Response[v1.GetFortunesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("fortune.v1.FortuneService.GetFortunes is not implemented"))
}
